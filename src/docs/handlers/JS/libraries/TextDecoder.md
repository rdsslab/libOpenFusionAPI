# `TextDecoder()`

[External Documentation](https://nodejs.org/api/util.html#class-textdecoder) 

Standard Web API TextDecoder constructor to decode a stream of bytes into a string (available globally in Node.js).

*   Returns: TextDecoder instance

