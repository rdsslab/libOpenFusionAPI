# `TextEncoder()`

[External Documentation](https://nodejs.org/api/util.html#class-textencoder) 

Standard Web API TextEncoder constructor to encode a string into a stream of bytes (Uint8Array) using UTF-8 (available globally in Node.js).

*   Returns: TextEncoder instance

